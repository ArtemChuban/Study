SELECT
    COUNTRY_ID,
    COUNTRY_NAME
FROM
    LOCATIONS
        NATURAL JOIN
    COUNTRIES

MINUS

SELECT
    COUNTRY_ID,
    COUNTRY_NAME
FROM
    DEPARTMENTS
        NATURAL JOIN
    LOCATIONS
        NATURAL JOIN
    COUNTRIES
;